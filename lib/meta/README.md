# lib/meta

Graph API client and Meta OAuth helpers.

Phase 2: OAuth flow + long-lived token exchange.
Phase 3: organic post + comment fetchers, with paging cursors and rate-limit
backoff driven by `X-App-Usage`.
Phase 6: ads enumeration → `effective_object_story_id` → ad comment fetchers.
